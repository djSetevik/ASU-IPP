<Window x:Class="ASUIPP.App.Views.AddTeacherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить преподавателя" Width="400" Height="160"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock DockPanel.Dock="Left" Text="ФИО:" Width="40" VerticalAlignment="Center"/>
            <TextBox x:Name="NameBox" Padding="5,3"/>
        </DockPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Добавить" Padding="20,6" Margin="0,0,10,0"
                    Click="Ok_Click" IsDefault="True"/>
            <Button Content="Отмена" Padding="20,6" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>