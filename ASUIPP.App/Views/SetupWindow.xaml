<Window x:Class="ASUIPP.App.Views.SetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="АСУИПП — Первоначальная настройка" Width="600" Height="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#C0C0C0">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Первоначальная настройка АСУИПП"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Шаг 1: Загрузка Excel -->
        <TextBlock Grid.Row="1" Text="1. Загрузите файл Excel с показателями:"
                   FontSize="14" Margin="0,0,0,8"/>

        <DockPanel Grid.Row="2" Margin="0,0,0,15">
            <Button DockPanel.Dock="Right" Content="Обзор..." Padding="15,5" Margin="10,0,0,0"
                    Command="{Binding BrowseExcelCommand}"/>
            <TextBox Text="{Binding ExcelFilePath, Mode=OneWay}" IsReadOnly="True"
                     Padding="5,5" Background="#E0E0E0"/>
        </DockPanel>

        <!-- Статус -->
        <TextBlock Grid.Row="4" Text="{Binding StatusText}" FontSize="12"
                   Foreground="Gray" TextWrapping="Wrap" Margin="0,0,0,15"/>

        <!-- Кнопка "Начать работу" -->
        <Button Grid.Row="5" Content="Начать работу" HorizontalAlignment="Right"
                Padding="25,8" FontSize="14"
                Command="{Binding FinishCommand}"
                IsEnabled="{Binding CanFinish}"/>
    </Grid>
</Window>